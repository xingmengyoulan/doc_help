```c
修改内容: pjcode/flow/chnl
 790         /*add by xyl for examining postgresql is normal or not --20170929*/
 791         if(  db_select() != OK )
 792         {
 793                 swVdebug(3,"sssssssss-zhaodong---------");
 794                 db_close();
 795                 if( (ret = db_open()) != OK )
 796                 {
 797                 swVdebug(3,"sssssssss----test----zhaodong---------");
 798                         swVdebug( 0, "db_open failed, ret[%d]", ret );
 799                         Fchg32((FBFR32 *)rcv_buf->data,QD_RESPCODE,0,ERR_ORA_EXCP,0);
 800 
 801                         snprintf( aWarning, 200,"sysid[%s] trancode[%s] db_open Err. SEQNO[%s]",sgTransdetail.aRecesysno,sgTransdetail.aRecepro
     dcode,sgTransdetail.aSendtranid); 802                         tcp_send2("0000","报文处理前","0000000002",aWarning);
 803                         tpreturn( TPSUCCESS, 0, rcv_buf->data, 0, 0 );
 804                 }
 805         }

chmng管理库 -> 使用一个读VIP刷共享内存 一个写的VIP应用访问

```

